{"default":{"identifier":"default","description":"Everything the main window is allowed to do. Folder/terminal/IDE opening is implemented Rust-side, so the webview needs only core IPC, events, and the native folder-pick dialog.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open"]}}